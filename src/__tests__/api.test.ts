import ConfigurationManager, { IConfiguration } from "../config";

describe("API", () => {
  describe("addNote", () => {
    test("Should add a note to the Notes table in the database", () => {
      console.log("")
    })
  })
})